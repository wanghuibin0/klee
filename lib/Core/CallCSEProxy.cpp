#include "CallCSEProxy.h"
#include "klee/Core/SummaryManager.h"
#include "Executor.h"

using namespace klee;

void CallCSEProxy::execute() {
  /* Summary *sum = summaryManager.getSummary(currentState, func); */
  /* if (sum && executor.checkCompatible(currentState, *sum)) { */
  /*   executor.applySummary(currentState, arguments, sum) */
  /* } */

}
